# 飞书配置

feishu:
  # 使用飞书开放平台 API（复用"家庭理财助手"机器人）
  chat_id: "oc_42357f35a9e99d9be4f1dd855a3ebc10"  # 理财小队
  
  # 或者使用 webhook URL（需要单独的机器人，不推荐）
  # webhook_url: "https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_WEBHOOK_TOKEN"

# 说明：
# - 使用 chat_id 方式会复用 OpenClaw 配置的"家庭理财助手"机器人
# - chat_id 可以通过运行 scripts/get_feishu_chats.py 获取
